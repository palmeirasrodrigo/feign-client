server:
  port: 8888
spring:
  application:
    name: rodrigo-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/palmeirasrodrigo/config-server
          #username:
          #password:
          default-label: main
          search-paths:
            - 'greeting-service*'